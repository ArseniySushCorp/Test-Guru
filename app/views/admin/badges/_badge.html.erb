<div class="col s3">
  <h6><%= badge.title %></h6>
  <%= link_to "Edit", edit_admin_badge_path(badge), class: "btn blue" %>
  <p><%= badge.description %></p>
  <%= tag.img src: image_url(badge.image), class: "badge-img" %>
</div>
