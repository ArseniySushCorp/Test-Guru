<nav>
  <div class="nav-wrapper light-blue">
    <div class="container">
      <div class="col s12">
        <%= link_to "Test guru Admin Panel", root_path, class: "brand-logo center" %>
        <ul class="right">
          <% if user_signed_in? %>
          <li>
            <%= current_user.login %> guru
          </li>
          <li>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'btn light-blue' %>
          </li>
          <li>
            <%= link_to "Badges", admin_badges_path, class: 'btn light-blue' %>
          </li>
          <% else %>
          <li>
            <%= link_to "Sign in", new_user_session_path %>
          </li>
          <li>
            <%= link_to "Sign up", new_user_registration_path, class: 'btn light-blue' %>
          </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>